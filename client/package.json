{
  "name": "messenger-client",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper around the Lustre client build",
  "scripts": {
    "gen:env": "node scripts/gen-env.mjs",
    "start": "npm run gen:env && gleam run -m lustre/dev start",
    "build:web": "npm run gen:env && gleam run -m lustre/dev build",
    "sync": "npm run build:web && cap sync",
    "android": "npm run sync && cap open android",
    "ios": "npm run sync && cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
