# This section will be overridden by eventual users of the library
[database]
# Dev-only credentials (same as compose.yaml). Use DATABASE_URL in real envs.
url = "postgres://messenger:messenger@localhost:5433/messenger"

# If other data in this section is defined, it is used to connect
# Otherwise, we fallback to the DATABASE_URL environment variable
# host = "localhost"
# user = "postgres"
# /!\ It is not recommended to have your password there
# password = "postgres"
# port = 5432
# name = "postgres"

# This section will be overridden by eventual users of the library
[migration-table]
# schema = "public"
# table = "migrations"

[migrations]
# migration_folder = "migrations"

[migrations.dependencies]

